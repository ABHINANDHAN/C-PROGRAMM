#include <stdio.h>
#include<math.h>
 
long power (inta, intb);
 
int main()
{
    int power=4, num=2;
    long result;
 
    printf("%d", a);
    scanf("%d", &b);
    printf("%d", a);
    scanf("%d", &a);
    result = power(num, pow);
    printf("%d^%d is %ld", b, a, result);
    return 0;
}
 
long power (int a, int b)
{
    if (a)
    {
        return (b * a(b ,a- 1));
    }
    return 1;
}
